version: '2'
services:
    gisimport:
        build:
            context: .
            dockerfile: Dockerfile_dev
        volumes:
            - "./target:/target"
        ports:
            - "8081:8080"
            - "8000:8000"
