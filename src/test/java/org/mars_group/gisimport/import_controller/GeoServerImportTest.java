package org.mars_group.gisimport.import_controller;

import org.junit.Test;
import org.mars_group.gisimport.util.UploadType;
import org.springframework.beans.factory.annotation.Autowired;

import java.io.File;

import static org.junit.Assert.*;

public class GeoServerImportTest {

    private static final String uploadDir = "upload-dir";

//    @Autowired
//    GeoServerImport gsImport;

    @Test
    public void handleShpImport() throws Exception {
//        String zipFilename = "src/test/resources/shapefile.zip";
//        File file = new File(uploadDir + "/" + zipFilename);
//        UploadType uploadType =UploadType.SHP;

//        gsImport.handleImport(uploadDir, zipFilename, uploadType);
    }
}
